{% extends 'base.html.twig' %}

{% block title %}PictureProduit{% endblock %}

{% block body %}
    <h1>PictureProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picture_produit.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ picture_produit.name ? picture_produit.name|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('picture_produit_index') }}">back to list</a>

    <a href="{{ path('picture_produit_edit', {'id': picture_produit.id}) }}">edit</a>

    {{ include('picture_produit/_delete_form.html.twig') }}
{% endblock %}
